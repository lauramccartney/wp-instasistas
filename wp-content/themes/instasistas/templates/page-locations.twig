{% extends "base.twig" %}

{% block content %}
	<main class="wrapper locations">

		{% for post in locations %}
			{% include ['tease-locations.twig', 'tease.twig'] %}
		{% endfor %}

		{% include 'partial/pagination.twig' with { pagination: posts.pagination({show_all: false, mid_size: 3, end_size: 2}) } %}

	</main>
{% endblock %}
